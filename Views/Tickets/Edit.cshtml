@model TicketFormViewModel
@{ ViewData["Title"] = "Edit Ticket"; }
<h1>Edit ticket #@Model.Id</h1><p class="text-muted">Update the ticket details or its status.</p><partial name="_TicketForm" model="Model" />
